{
  "name": "slow-http-server",
  "version": "1.0.0",
  "description": "A simple, not-at-all-fast http server for serving static files and browsing directories.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist && webpack --config webpack.config.js --mode development",
    "start": "nodemon index.js",
    "watch": "watch 'npm run build' src"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-react": "^6.24.1",
    "nodemon": "^1.17.3",
    "watch": "^1.0.2",
    "webpack": "^4.5.0",
    "webpack-cli": "^2.0.14"
  },
  "dependencies": {
    "express": "^4.17.3",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "uuid": "^3.2.1"
  }
}
