To run, you must compile the frontend and run the server. 

### Frontend (React app)
`npm run build` or `npm run watch`

### Server
`npm run start` 
or
`export CONTENT_DIRECTORY=/some/path; export PORT=1234; npm run start`
